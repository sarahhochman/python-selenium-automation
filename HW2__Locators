# Amazon Logo
driver.find_element(By.XPATH, "//i[@role='img' and @aria-label='Amazon']")

# email field
driver.find_element(By.ID, "ap_email")

#continue button
driver.find_element(By.ID, "continue")

#need help link (you could also use class, but this explains better, and I used
# just contains text because the grammar in the whole thing is wrong so may be fixed in the future.
driver.find_element(By.XPATH, "//span[contains(text(), 'Need')]")

#Forgot your password link, this would need a comment to explain as the id does not
driver.find_element(By.ID, "auth-fpp-link-bottom")

#other issues with sign in link
driver.find_element(By.ID, "ap-other-signin-issues-link")

#create amazon account
driver.find_element(By.ID, "createAccountSubmit")

#conditions of use link
driver.find_element(By.XPATH, "//a[contains(@href, 'condition_of_use')]")

#privacy notice link
driver.find_element(By.XPATH, "//a[contains(@href, 'notification_privacy_notice')]")
